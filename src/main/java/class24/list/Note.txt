methods from the arrayList class
do not use Iterators use removeif();

ArrayList:
    if in your code 80% of the times you use get method,
                    20% add(index,element), remove.
LinkedList
    if 70-80% you use remove method or adding the data in-between you should use linkedList


    CD framework is a pre-written code that allows developers to build applications more easily and quickly.